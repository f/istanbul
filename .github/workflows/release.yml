name: Build & Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Get version from tag
        id: version
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          VERSION="${TAG#v}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "number=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Update app version in Xcode project
        run: |
          sed -i '' "s/MARKETING_VERSION = .*;/MARKETING_VERSION = ${{ steps.version.outputs.number }};/g" Istanbul.xcodeproj/project.pbxproj

      - name: Commit version bump
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Istanbul.xcodeproj/project.pbxproj
          git commit -m "Bump version to ${{ steps.version.outputs.number }}" || echo "No changes to commit"
          git push origin HEAD:refs/heads/main || echo "Push skipped"

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.2.app/Contents/Developer

      - name: Build universal app
        run: |
          chmod +x build.sh
          ./build.sh

      - name: Upload DMG to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: build/release/Istanbul.dmg
          name: Istanbul ${{ steps.version.outputs.tag }}
          draft: false
          prerelease: false
          generate_release_notes: true
          body: |
            ## Installation

            1. Download **Istanbul.dmg** below
            2. Open the DMG and drag **Istanbul** to Applications
            3. Since the app is not notarized, macOS may block it on first launch. Run the following command in Terminal to fix this:

            ```
            xattr -cr /Applications/Istanbul.app
            ```

            Then open Istanbul normally.

            Or install via Homebrew:

            ```
            brew install f/tap/istanbul
            ```

      - name: Compute DMG SHA256
        id: sha
        run: echo "sha256=$(shasum -a 256 build/release/Istanbul.dmg | awk '{print $1}')" >> "$GITHUB_OUTPUT"

      - name: Update Homebrew Cask tap
        env:
          TAP_REPO_TOKEN_HOMEBREW: ${{ secrets.TAP_REPO_TOKEN_HOMEBREW }}
          VERSION: ${{ steps.version.outputs.tag }}
          SHA256: ${{ steps.sha.outputs.sha256 }}
        run: |
          VERSION_NUM="${VERSION#v}"
          git clone https://x-access-token:${TAP_REPO_TOKEN_HOMEBREW}@github.com/f/homebrew-tap.git tap
          mkdir -p tap/Casks
          cat > tap/Casks/istanbul.rb << EOF
          cask "istanbul" do
            version "${VERSION_NUM}"
            sha256 "${SHA256}"

            url "https://github.com/f/istanbul/releases/download/${VERSION}/Istanbul.dmg"
            name "Istanbul"
            desc "macOS menu bar app for ambient Istanbul soundscapes from BBC Rewind"
            homepage "https://github.com/f/istanbul"

            depends_on macos: ">= :sonoma"

            app "Istanbul.app"

            postflight do
              system_command "/usr/bin/xattr", args: ["-cr", "#{appdir}/Istanbul.app"]
            end

            zap trash: [
              "~/Library/Preferences/dev.fka.Istanbul.plist",
              "~/Library/Saved Application State/dev.fka.Istanbul.savedState",
              "~/Library/Caches/IstanbulSounds",
            ]
          end
          EOF
          cd tap
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Update Istanbul to ${VERSION}"
          git push
